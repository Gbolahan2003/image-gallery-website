/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com','firebasestorage.googleapis.com', 'www.pexels.com', 'images.app.goo.gl',  'images.unsplash.com'],
      },
};

export default nextConfig;
